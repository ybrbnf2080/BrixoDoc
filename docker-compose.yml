version: '3.7'

services:
  web:
    build: .
    command:  sh -c "python manage.py migrate && gunicorn BrixoDoc.wsgi:application --bind 0.0.0.0:8000 --reload -w 4"
    volumes:
      - ./:/usr/src/app/
      - static_volume:/usr/src/app/static
    ports:
      - "30060:30060"
    env_file:
      - ./.env.dev
    depends_on:
      - db
  db:
    image: postgres:12.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=haeryr^sdf^dfg
      - POSTGRES_DB=admin
  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
    volumes:
      - ./nginx/cert:/etc/ssl/cert
      - static_volume:/usr/src/app/static
volumes:
  postgres_data:
  static_volume:
